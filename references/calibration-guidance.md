# Calibration guidance

Calibration evaluates a model repeatedly; it does not implement the model. Use a user-specified objective when supplied. The baseline objective is KGE and diagnostics should also report NSE, RMSE and PBIAS. Pareto selection is an advanced mode: its objectives and selection rule must be specified explicitly.

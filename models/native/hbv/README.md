# Native HBV reference model

The initial implementation is a compact daily conceptual HBV-style model with snow, soil-moisture, upper-zone and lower-zone stores. It exists to validate the stable model interface, not to claim a production-calibrated HBV variant. Parameters, units and equation choices are recorded in the run result.
